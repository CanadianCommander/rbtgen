
export enum FieldType
{
  STRING = "string",
  INTEGER = "integer",
  FLOAT = "float",
  DATE = "date",
  DATE_TIME = "date_time",
  TIME = "time",
  BINARY = "binary",
  ENUM = "enum",
  CUSTOM = "custom",
}
